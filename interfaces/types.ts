interface ElemntBox {
    a: number,  // top-left corner of the bounding box (px)
    b: number,  // top-right corner of the bounding box (px)
    c: number,  // bottom-left corner of the bounding box (px)
    d: number,  // bottom-right corner of the boudning box (px)
    W: number,  // width of the boudning box (px)
    H: number,  // height of the boudning box (px)
}